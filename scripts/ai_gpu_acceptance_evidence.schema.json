{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autoedit.local/schemas/ai-gpu-1-acceptance-harness-evidence-1.0.0.json",
  "title": "AUTOEDIT AI-GPU-1 acceptance harness evidence (canonical reference)",
  "description": "This implementation schema is proven semantically identical to the canonical contract at ../docs/plans/ai-gpu-1-redacted-evidence.schema.json (version 1.0.0). The harness loads, validates, and re-emits the canonical schema directly; this file exists only so the Programmer-owned scripts/ tree carries an explicit, resolvable contract reference. additionalProperties stays closed in the canonical schema so no raw payload can hide in the record.",
  "$ref": "../docs/plans/ai-gpu-1-redacted-evidence.schema.json"
}
